{% extends 'base.html.twig' %}

{% block title %}Liste des livres{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1>📚 Liste des livres</h1>
        <a class="btn btn-primary" href="{{ path('app_book_new') }}">Créer un livre</a>
    </div>

    <div class="table-responsive">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Titre</th>
                    <th>Auteur</th>
                    <th>Publication</th>
                    <th>Publié</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for book in books %}
                <tr>
                    <td>{{ book.id }}</td>
                    <td>{{ book.title }}</td>
                    <td>{{ book.author ? book.author.username : '-' }}</td>
                    <td>{{ book.publicationDate ? book.publicationDate|date('Y-m-d') : '-' }}</td>
                    <td>{{ book.enabled ? 'Yes' : 'No' }}</td>
                    <td>
                        <a class="btn btn-sm btn-outline-secondary" href="{{ path('app_book_show', {'id': book.id}) }}">Show</a>
                        <a class="btn btn-sm btn-outline-primary" href="{{ path('app_book_edit', {'id': book.id}) }}">Edit</a>
                        <form method="post" action="{{ path('app_book_delete', {'id': book.id}) }}" style="display:inline-block" onsubmit="return confirm('Delete this book?');">
                            <button class="btn btn-sm btn-danger">Delete</button>
                        </form>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5" class="text-center text-muted py-4">Aucun livre trouvé.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}

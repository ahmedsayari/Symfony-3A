{% extends 'base.html.twig' %}

{% block title %}Modifier un lecteur{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Modifier le lecteur #{{ reader.id }}</h1>

    {{ form_start(form) }}
        {{ form_row(form.username) }}
        {{ form_row(form.save) }}
    {{ form_end(form) }}

    <a class="btn btn-link mt-3" href="{{ path('app_reader_index') }}">Retour à la liste</a>
</div>
{% endblock %}
